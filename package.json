{
  "name": "expand-hex-code",
  "version": "1.1.0",
  "description": "Expand hex codes from three to six digits",
  "main": "lib/expand-hex-code.js",
  "repository": "https://github.com/nielse63/expand-hex-code",
  "author": {
    "name": "Erik Nielsen",
    "email": "erik@312development.com",
    "url": "https://312development.com"
  },
  "license": "MIT",
  "homepage": "https://github.com/nielse63/expand-hex-code",
  "keywords": [
    "css",
    "hex",
    "code",
    "expand"
  ],
  "bugs": "https://github.com/nielse63/expand-hex-code/issues",
  "scripts": {
    "clean": "./scripts/clean",
    "lint": "eslint src/ test/ --cache --fix",
    "changelog": "github-changes -o nielse63 -r ${npm_package_name} -n ${npm_package_version} --only-pulls --use-commit-body",
    "prebuild": "yarn run lint && yarn run clean",
    "build": "babel src/ -d lib/",
    "pretest": "yarn run build",
    "test": "nyc mocha test/specs/*.spec.js",
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "preversion": "yarn test",
    "version": "yarn run changelog && git add CHANGELOG.md",
    "prepublishOnly": "yarn run build",
    "commitmsg": "validate-commit-msg",
    "precommit": "yarn run lint",
    "prepush": "yarn test"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-eslint": "^7.2.3",
    "babel-plugin-istanbul": "^4.1.5",
    "babel-preset-env": "^1.6.0",
    "babel-preset-flow": "^6.23.0",
    "babel-preset-stage-2": "^6.24.1",
    "chai": "^4.1.1",
    "chai-asserttype": "^1.0.5",
    "conventional-commit-types": "^2.2.0",
    "coveralls": "^2.13.1",
    "eslint": "^4.5.0",
    "eslint-config-prettier": "^2.9.0",
    "eslint-config-xo": "^0.19.0",
    "eslint-plugin-flowtype": "^2.40.1",
    "eslint-plugin-import": "^2.7.0",
    "eslint-plugin-prettier": "^2.4.0",
    "flow-bin": "^0.61.0",
    "github-changes": "^1.1.1",
    "husky": "^0.14.3",
    "mocha": "^3.5.0",
    "nyc": "^11.1.0",
    "prettier": "^1.9.2",
    "validate-commit-msg": "^2.14.0"
  },
  "engines": {
    "node": ">= 8.x",
    "npm": ">= 5.x",
    "yarn": ">= 1.x"
  },
  "config": {
    "validate-commit-msg": {
      "types": "conventional-commit-types",
      "subjectPatternErrorMsg": "[validate-commit-msg]: Subject does not match subject pattern!",
      "autoFix": true
    }
  }
}
