{
  "name": "expand-hex-code",
  "version": "1.0.5",
  "description": "Expand hex codes from three to six digits",
  "main": "index.js",
  "repository": "https://github.com/nielse63/expand-hex-code",
  "author": {
    "name": "Erik Nielsen",
    "email": "erik@312development.com",
    "url": "https://312development.com"
  },
  "license": "MIT",
  "homepage": "https://github.com/nielse63/expand-hex-code",
  "keywords": [
    "css",
    "hex",
    "code",
    "expand"
  ],
  "bugs": "https://github.com/nielse63/expand-hex-code/issues",
  "scripts": {
    "lint": "eslint src ./*.js --ignore-pattern '!.eslintrc.js'",
    "lint-fix": "npm run lint -- --fix",
    "minify": "uglifyjs lib/expand-hex-code.js -m -c -o lib/expand-hex-code.min.js",
    "rollup": "rollup --input lib/expand-hex-code.js --output.format umd --output.file lib/expand-hex-code.js",
    "prebuild": "npm run lint && rm -rf lib",
    "build": "babel src -d lib --source-maps inline",
    "postbuild": "npm run rollup && npm run minify",
    "pretest": "rm -rf .nyc_output coverage *.log",
    "test": "nyc mocha test/index.js",
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "prepublishOnly": "npm test && npm run build"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-eslint": "^7.2.3",
    "babel-preset-env": "^1.6.0",
    "babel-register": "^6.26.0",
    "chai": "^4.1.1",
    "chai-asserttype": "^1.0.5",
    "coveralls": "^2.13.1",
    "eslint": "^4.5.0",
    "eslint-config-airbnb-base": "^11.3.2",
    "eslint-plugin-import": "^2.7.0",
    "mocha": "^3.5.0",
    "nyc": "^11.1.0",
    "rollup": "^0.48.2",
    "uglify-js": "^3.0.28"
  },
  "engines": {
    "node": ">= 8.x",
    "npm": ">= 5.x"
  }
}
