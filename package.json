{
  "name": "expand-hex-code",
  "version": "1.0.4",
  "description": "Expand hex codes from three to six digits",
  "main": "index.js",
  "repository": "https://github.com/nielse63/expand-hex-code",
  "author": {
    "name": "Erik Nielsen",
    "email": "erik@312development.com",
    "url": "https://312development.com"
  },
  "license": "MIT",
  "homepage": "https://github.com/nielse63/expand-hex-code",
  "keywords": [
    "css",
    "hex",
    "code",
    "expand"
  ],
  "bugs": "https://github.com/nielse63/expand-hex-code/issues",
  "scripts": {
    "lint": "eslint src/ test/ config/ ./*.js --ignore-pattern '!.eslintrc.js'",
    "prebuild": "yarn run lint --fix && rm -rf build/",
    "build:dev": "cross-env NODE_ENV=development webpack --config config/webpack.dev.config.js",
    "build:prod": "cross-env NODE_ENV=production webpack --config config/webpack.prod.config.js",
    "build": "yarn run build:dev && yarn run build:prod",
    "pretest": "rm -rf .nyc_output coverage *.log",
    "test": "nyc mocha test/specs/*.spec.js",
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "prepublishOnly": "yarn test && yarn run build"
  },
  "devDependencies": {
    "babel-eslint": "^7.2.3",
    "babel-loader": "^7.1.2",
    "babel-plugin-istanbul": "^4.1.5",
    "babel-preset-env": "^1.6.0",
    "babel-preset-stage-2": "^6.24.1",
    "chai": "^4.1.1",
    "chai-asserttype": "^1.0.5",
    "coveralls": "^2.13.1",
    "cross-env": "^5.1.1",
    "eslint": "^4.5.0",
    "eslint-config-airbnb-base": "^11.3.2",
    "eslint-loader": "^1.9.0",
    "eslint-plugin-import": "^2.7.0",
    "mocha": "^3.5.0",
    "nyc": "^11.1.0",
    "webpack": "^3.10.0",
    "webpack-merge": "^4.1.1"
  },
  "engines": {
    "node": ">= 8.x",
    "npm": ">= 5.x",
    "yarn": ">= 1.x"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not ie <= 8"
  ]
}
